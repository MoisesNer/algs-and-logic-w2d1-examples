// .concat merges two arrays and return a new array. does not
// change the original arrays. 

let nums = [1, 4, 7, 8];

let animals = ["bird", "dog", "whale", 'Jellyfish']

console.log(nums.concat(animals));

// console.log(newArr)