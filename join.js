// lets you take every item in an array, and add together all
// of the array elements with a given seperator character

let cars = ['ford', 'BMW', 'Audi', 'Ferarri'];

let joinedCars = cars.join('-');

console.log(joinedCars);